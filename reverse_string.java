package com.company;

public class reverse_string {


    public static void reverse_string (char [] str){


        }




    public static void main(String[] args) {
    //using for loop
        /* String str = "sagar";
     String rstr="";
     char temp;
     for(int i =0; i<=str.length(); i++){
         temp = str.charAt(i);
         rstr=temp+rstr;

     */
        /*
        //using to charArray() method of String class
        String str = "sagar";
        char[] try1 = str.toCharArray();
        for (int i=str.length()-1; i>=0; i--){
            System.out.print(try1[i]);

         */

        /*
//using conversion of string into charArray and swapping the position of characters

        String str = "sagar is a good boy";
        char [] tempArray =str.toCharArray();
        int left=0;
        int right =tempArray.length-1;
        char temp;
        for (int i=0; left<right; left++,right--){
            temp = tempArray[left];
            tempArray[left]=tempArray[right];
            tempArray[right] = temp;
     }
        for(char arr: tempArray){
            System.out.print(arr);
            System.out.print("");
        }
*/
        
    }

}
